# US-014: Final polish: README, error handling, and startup experience

**Priority:** 14
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, MiniPanel has a polished first-run experience: clear README, auto-creating database, friendly empty states, consistent error responses, request logging, and no crashes from unhandled errors. ALL Playwright tests must pass.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- README.md contains: project description (one paragraph), prerequisites (Node.js version), setup instructions (npm install, npm run seed, npm run dev), explanation of what each command does
- Running 'npm run dev' on a fresh clone (after npm install) starts without errors even if the database does not exist yet (auto-creates it)
- If the database is empty, the Events page shows a friendly empty state with a hint to run 'npm run seed'
- API endpoints return consistent error response format: { error: string, details?: string }
- Server logs incoming requests with method, path, and status code (simple middleware)
- No unhandled promise rejections or uncaught exceptions crash the server
- Typecheck passes
- ALL Playwright tests pass: npx playwright test

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "This is the cleanup story. Don't add new features — just make everything that exists work smoothly."

## Dependencies

- **Hard dependencies:**
  - US-010 — Events page must exist for empty state enhancement
  - US-012 — User lookup page must exist
- **Soft dependencies:**
  - All prior stories — this is a polish pass, best done after features land
- **Unblocks:** Nothing — this is the final Tier 1/2 story.

## Expected artifacts (for the coder)

- `README.md` — rewrite with full documentation
- `server/src/index.ts` — request logging middleware, error handling middleware
- `client/src/pages/Events.tsx` — empty state with seed hint
- Database migrations? No
- Config changes? No

## Test scope (for the validator)

- Primary test file: `e2e/US-014.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - README.md contains setup instructions
  - Server starts on fresh clone without errors
  - Empty database → Events page shows seed hint
  - API errors return { error: string } format
  - Request logging middleware works
  - ALL existing Playwright tests pass
- Edge cases worth adversarial testing:
  - Invalid JSON body returns proper error, not crash
  - 404 for unknown routes returns JSON error
  - Server handles concurrent requests without crashing

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
