# US-010: Build event explorer page with table and filters

**Priority:** 10
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, MiniPanel has a functional event explorer page where analysts can browse events in a table, filter by event name and date range, expand rows to see properties, and paginate through results.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- Events page is accessible from the sidebar navigation
- Displays a table of events with columns: Timestamp, Event Name, User (resolved identity), Properties (truncated preview)
- Table loads data from GET /api/events on mount
- Filter bar above the table with: event name dropdown (populated from distinct event names in the data), date range picker (start and end date inputs), a search/filter button
- Filters update the API query parameters and reload the table
- Clicking a row expands it or opens a detail panel showing all event properties formatted as key-value pairs
- Pagination controls at the bottom (Previous / Next) using offset-based pagination
- Loading state shown while fetching data
- Empty state shown when no events match the filters
- Uses shadcn/ui Table component and lucide-react icons
- Typecheck passes
- Verify in browser using dev-browser skill
- Events page UI tests pass: npx playwright test ui-events.spec.ts

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "This is the primary interface for the developer persona and the analyst persona. Keep the UI functional, not flashy."

## Dependencies

- **Hard dependencies:**
  - US-007 — needs GET /api/events with filters and pagination
  - US-009 — needs app shell with navigation
- **Soft dependencies:**
  - US-008 — seed data makes the page useful for demo
  - US-011 — event name dropdown needs GET /api/events/names (can fall back to hardcoded or fetched from events)
- **Unblocks:** US-019 (BR-200 gap-check), US-014 (polish)

## Expected artifacts (for the coder)

- `client/src/pages/Events.tsx` — replace placeholder with full event explorer
- `client/src/components/EventTable.tsx` (or similar) — table component with expand
- Database migrations? No
- Config changes? No

## Test scope (for the validator)

- Primary test file: `e2e/US-010.spec.ts` or run `e2e/ui-events.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - Events page loads and renders table with event data
  - Table has columns: Timestamp, Event Name, User identity, Properties preview
  - Filter by event name updates table
  - Filter by date range updates table
  - Pagination Next/Previous works
  - Row expand shows all event properties
  - Empty state when no events match filters
  - Loading indicator visible during fetch
- Edge cases worth adversarial testing:
  - Table with 0 events shows empty state, not broken layout
  - Very long property values are truncated in preview

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
