# US-008: Build sample data seeder script

**Priority:** 8
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, running `npm run seed` populates MiniPanel with 10,000+ realistic demo events across 50+ users, with identity resolution scenarios, non-uniform distributions, and varied properties. New users can explore the product immediately without instrumenting their own app.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- A script at 'scripts/seed.ts' (or similar) can be run via 'npm run seed' from the project root
- Generates at least 5 distinct event types: Page Viewed, Button Clicked, Sign Up Completed, Purchase Completed, Subscription Renewed
- Generates at least 50 resolved users (users with a user_id) and additional anonymous-only device IDs
- Generates at least 10,000 events spread over the last 30 days
- Distribution is non-uniform: some users have 500+ events, most have 50-200, power-law or similar distribution
- Event types are non-uniform: Page Viewed is most common (~40%), Button Clicked (~25%), Sign Up Completed (~15%), Purchase Completed (~12%), Subscription Renewed (~8%)
- Includes identity resolution scenarios: at least 20 users start as anonymous (device-only) and later identify, at least 5 users have 2+ devices, at least 10 device IDs never get mapped to a user
- Events include string properties (url, referrer, button_name, plan_type) and numeric properties (amount, duration_seconds, quantity) appropriate to each event type
- Seeder calls the batch API endpoint (POST /api/events/batch) or directly inserts into the database
- Running the seeder twice does not duplicate data (clears existing data first or is idempotent)
- Typecheck passes
- After seeding, existing query tests still pass: npx playwright test api-queries.spec.ts

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "The seeder should be deterministic (use a fixed seed for the random number generator) so results are reproducible. Direct database insertion is fine and will be much faster than HTTP calls for 10k events."

## Dependencies

- **Hard dependencies:**
  - US-004 — needs batch API or direct DB access
  - US-007 — query tests must still pass after seeding
- **Soft dependencies:** None.
- **Unblocks:** US-010 (soft — demo data for event explorer)

## Expected artifacts (for the coder)

- `scripts/seed.ts` — seeder script
- Root `package.json` — `npm run seed` script
- Database migrations? No
- Config changes? Root package.json script

## Test scope (for the validator)

- Primary test file: `e2e/US-008.spec.ts` or `server/src/__tests__/US-008.test.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - `npm run seed` executes without error
  - After seeding, total events >= 10,000
  - At least 5 distinct event types present
  - At least 50 resolved users
  - Identity mappings exist (at least 20 device→user mappings)
  - Running seed twice doesn't duplicate (idempotent)
  - Existing query tests still pass post-seed
- Edge cases worth adversarial testing:
  - Event distribution is non-uniform (not all events equal count)
  - Numeric properties exist on relevant event types (amount on Purchase Completed)
  - Events spread over 30 days (not all on one day)

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
