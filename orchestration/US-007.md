# US-007: Implement event listing API with filters

**Priority:** 7
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, the frontend can query events with filters (event name, date range, identity) and pagination. This is the read API that powers the event explorer page (US-010).

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- GET /api/events returns paginated events sorted by timestamp descending (newest first)
- Supports query parameters: event_name (filter by exact event name), user_id (resolved identity filter), device_id (resolved identity filter), start_date (ISO 8601, inclusive), end_date (ISO 8601, inclusive), limit (default 50, max 1000), offset (default 0)
- All filters are optional and combinable
- Response body: { events: Event[], total: number, limit: number, offset: number }
- Events in response have properties parsed from JSON string to object
- Identity resolution is applied: if filtering by user_id, events from all mapped devices are included
- Typecheck passes
- GET /api/events filter and pagination tests pass in api-queries.spec.ts (the GET /api/events/names and GET /api/stats/overview tests may still fail — those are addressed in US-011)

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "Build the SQL query dynamically based on which filters are provided."

## Dependencies

- **Hard dependencies:**
  - US-005 — needs identity resolution for user_id/device_id filters
- **Soft dependencies:** None.
- **Unblocks:** US-008 (seeder tests query API), US-010 (event explorer page), US-019 (BR-200 gap-check)

## Expected artifacts (for the coder)

- `server/src/routes/events.ts` — enhance GET /api/events with filters, pagination, sorting
- Database migrations? No
- Config changes? No

## Test scope (for the validator)

- Primary test file: `e2e/US-007.spec.ts` or run `e2e/api-queries.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - Default response has { events, total, limit: 50, offset: 0 }
  - Events sorted by timestamp descending (newest first)
  - Filter by event_name returns only matching events
  - Filter by start_date and end_date returns events in range (inclusive)
  - Pagination with limit and offset returns correct slices
  - Combined filters apply AND logic
  - Identity resolution applied for user_id filter
- Edge cases worth adversarial testing:
  - Offset beyond total returns empty events array with correct total
  - Limit > 1000 clamped or rejected
  - No filters returns all events (up to default limit)

## Risk notes

Note the sort order difference: GET /api/events (this endpoint) sorts descending (newest first) for the paginated explorer view. GET /api/events?user_id=X from US-005 sorts ascending (oldest first) for the user timeline view. The coder must handle both cases correctly — spec is silent on whether these are the same route with different behavior or separate code paths. Defer to implementation judgment.

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
