# US-004: Implement batch event ingestion endpoint

**Priority:** 4
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, developers can POST up to 1000 events in a single request via `POST /api/events/batch`. Valid events are inserted in a transaction; invalid events are skipped and reported. This is essential for the sample data seeder (US-008).

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- POST /api/events/batch accepts a JSON body with field: events (array of event objects, same schema as single event)
- Validated with Zod: array must have at least 1 event and at most 1000
- Each event in the array is validated individually with the same rules as POST /api/events
- All valid events in the batch are inserted in a single SQLite transaction for performance
- Identity mappings are created for any events with both device_id and user_id
- If an event in the batch would create a device_id mapping that conflicts with an existing mapping (device already mapped to a different user), that individual event is skipped and reported in the errors array — it does NOT abort the entire batch
- Returns 200 with a JSON body: { accepted: number, errors: Array<{ index: number, message: string }> }
- If any individual event fails validation or has an identity conflict, it is skipped (not inserted) and reported in the errors array, but the rest of the batch succeeds
- Typecheck passes
- All Playwright event API tests pass: npx playwright test api-events.spec.ts (single + batch tests)

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "Essential for the sample data seeder (US-008) to run fast. Wrap the whole insert loop in a transaction."

## Dependencies

- **Hard dependencies:**
  - US-003 — needs single event validation logic and identity mapping logic to reuse
- **Soft dependencies:** None.
- **Unblocks:** US-008 (seeder uses batch endpoint)

## Expected artifacts (for the coder)

- `server/src/routes/events.ts` — add POST /api/events/batch handler
- Reuse Zod schema and identity conflict logic from US-003
- Database migrations? No
- Config changes? No

## Test scope (for the validator)

- Primary test file: `e2e/US-004.spec.ts` or run existing `e2e/api-events.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - Batch of valid events returns 200 with { accepted: N, errors: [] }
  - Mix of valid and invalid events: accepted = valid count, errors has entries for invalid ones
  - Empty array returns 400
  - Array with >1000 events returns 400
  - Identity conflict in batch skips that event but doesn't abort batch
  - Identity mappings created for events with both device_id and user_id
- Edge cases worth adversarial testing:
  - All events in batch invalid — returns 200 with accepted: 0 and full errors array
  - Batch with duplicate device_id→user_id pairs (same mapping repeated) — should succeed
  - Batch where first event creates mapping, later event conflicts — first accepted, later skipped

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
