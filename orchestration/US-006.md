# US-006: Write automated tests for identity resolution

**Priority:** 6
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, the codebase has automated unit/integration tests that verify the four core identity resolution scenarios. This satisfies the hard constraint that identity resolution must have automated test coverage.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- Test file exists at server/src/__tests__/identity-resolution.test.ts (or similar path using vitest or jest)
- Test runner is configured and 'npm test' in the server workspace runs the tests
- Test 1 - Retroactive merge: send 4 anonymous events for device-X, then send 1 event with both device-X and user-Y, query events for user-Y, assert all 5 events are returned
- Test 2 - Multi-device merge: send anonymous events for device-A and device-B, link device-A to user-Z, link device-B to user-Z, query events for user-Z, assert events from both devices appear
- Test 3 - Device collision rejection: link device-C to user-P, attempt to link device-C to user-Q, assert the second mapping is rejected (409 or error)
- Test 4 - Unidentified device: send anonymous events for device-D (no mapping), query by device-D, assert only device-D events are returned with device-D as the identity
- All tests pass
- Typecheck passes

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "Use a fresh in-memory SQLite database per test to avoid test pollution. This satisfies the hard constraint."

## Dependencies

- **Hard dependencies:**
  - US-005 — needs identity resolution logic to exist before testing it
- **Soft dependencies:** None.
- **Unblocks:** Nothing directly — but provides regression safety for all downstream stories.

## Expected artifacts (for the coder)

- `server/src/__tests__/identity-resolution.test.ts` — test file with 4 test cases
- `server/vitest.config.ts` or `server/jest.config.ts` — test runner config (if not already set up)
- `server/package.json` — `npm test` script
- Database migrations? No
- Config changes? Test runner config if needed

## Test scope (for the validator)

- Primary test file: `server/src/__tests__/US-006.test.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - `npm test` in server workspace executes and passes
  - Test file exists at specified path
  - All 4 test cases are present and pass
- Edge cases worth adversarial testing:
  - Tests use isolated databases (not shared state)
  - Test runner is properly configured (vitest or jest)

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
