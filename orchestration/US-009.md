# US-009: Build application shell with navigation layout

**Priority:** 9
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, MiniPanel has a clean application shell with a fixed sidebar containing navigation links (Events, Funnels, Users, Settings), React Router for routing, and placeholder pages for unimplemented routes.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- App renders a responsive layout with a fixed sidebar on the left and a main content area on the right
- Sidebar contains navigation links with lucide-react icons for: Events (Activity icon), Funnels (Filter icon, links to placeholder), Users (Users icon, links to placeholder), Settings (Settings icon, links to placeholder)
- React Router is set up with routes for each navigation item
- Active navigation item is visually highlighted
- Placeholder pages display the page title in the main content area for routes not yet implemented
- Application title 'MiniPanel' appears at the top of the sidebar
- Layout uses Tailwind CSS and looks clean on screens 1024px and wider
- Typecheck passes
- Verify in browser using dev-browser skill
- Navigation tests pass: npx playwright test ui-navigation.spec.ts

## Spec anchors (READ these sections if the briefing is unclear)

- prd.json notes — "Use shadcn/ui components where appropriate. Keep it minimal — this is the shell, not the feature pages."

## Dependencies

- **Hard dependencies:**
  - US-001 — needs client/ scaffold with React, Tailwind, shadcn/ui, lucide-react
- **Soft dependencies:** None.
- **Unblocks:** US-010 (event explorer), US-012 (user lookup), US-017 (trends page), US-029 (funnels page)

## Expected artifacts (for the coder)

- `client/src/App.tsx` — layout with sidebar + main content area, React Router setup
- `client/src/components/Sidebar.tsx` (or similar) — navigation component
- `client/src/pages/Events.tsx` — placeholder (replaced by US-010)
- `client/src/pages/Funnels.tsx` — placeholder
- `client/src/pages/Users.tsx` — placeholder
- `client/src/pages/Settings.tsx` — placeholder
- Database migrations? No
- Config changes? No

## Test scope (for the validator)

- Primary test file: `e2e/US-009.spec.ts` or run `e2e/ui-navigation.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - Sidebar displays 'MiniPanel' title
  - Navigation links for Events, Users, Funnels, Settings present
  - Clicking each nav link navigates to correct route
  - Active nav item has distinct styling
  - Placeholder pages display page title
- Edge cases worth adversarial testing:
  - Direct URL navigation (e.g., navigating to /funnels directly) works
  - Sidebar is fixed (doesn't scroll with content)

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
