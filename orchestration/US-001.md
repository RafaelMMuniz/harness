# US-001: Initialize project with monorepo structure

**Priority:** 1
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, the project has a working monorepo with Express+TypeScript backend and React+Vite+Tailwind frontend that both compile and start from a single `npm run dev` command. This is the foundation every other story builds on.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- Project root contains a package.json with a single 'npm run dev' command that starts both backend and frontend concurrently
- Backend: Express.js + TypeScript in a 'server/' directory, compiles and starts on port 3001
- Frontend: React + Vite + TypeScript + TailwindCSS in a 'client/' directory, starts on port 5173 and proxies API requests to backend
- Shadcn UI initialized in client with at least one component (Button) available
- lucide-react installed and importable in client
- Zod installed and importable in server
- Playwright installed with existing test files in 'e2e/' intact — do NOT modify or delete any files in e2e/
- A single 'npm install' at the root installs all dependencies (workspaces or similar)
- README.md documents: 'npm install && npm run dev' as the single start command
- Typecheck passes for both client and server
- Playwright config is valid: npx playwright test --list succeeds

## Spec anchors (READ these sections if the briefing is unclear)

- CLAUDE.md § "Where to look" — project layout conventions
- AGENTS.md — build/run/test commands (will be populated by this story)
- prd.json notes field — "This is the skeleton. Keep it minimal — no business logic yet."

## Dependencies

- **Hard dependencies:** None — this is the root story.
- **Soft dependencies:** None.
- **Unblocks:** US-002 (DB setup), US-009 (app shell), US-016 (date presets utility)

## Expected artifacts (for the coder)

- `package.json` (root) — workspaces config, `npm run dev` script using concurrently
- `server/package.json` — Express, TypeScript, better-sqlite3, zod deps
- `server/tsconfig.json` — TypeScript config
- `server/src/index.ts` — minimal Express server on port 3001
- `client/package.json` — React, Vite, Tailwind, shadcn/ui, lucide-react deps
- `client/vite.config.ts` — Vite config with API proxy to localhost:3001
- `client/tsconfig.json` — TypeScript config
- `client/src/main.tsx` — React entry point
- `client/src/App.tsx` — minimal app component
- `README.md` — setup instructions
- Database migrations? No
- Config changes? Root package.json, playwright.config.ts must remain valid

## Test scope (for the validator)

- Primary test file: `e2e/US-001.spec.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - `npm run dev` starts both servers (backend on 3001, frontend on 5173)
  - Root `npm install` installs all dependencies
  - TypeScript compilation passes for both workspaces
  - Playwright config is valid (`npx playwright test --list` succeeds)
  - Vite proxy forwards `/api` requests to backend
- Edge cases worth adversarial testing:
  - Backend returns a response on port 3001 (not just starts without error)
  - Frontend loads in browser on port 5173
  - shadcn Button component is importable (not just installed)

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
