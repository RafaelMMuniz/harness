# US-002: Set up SQLite database with connection and schema management

**Priority:** 2
**Status:** passes:false (briefing generated during --fresh)

## What this story accomplishes

After this story lands, the server automatically creates and initializes a SQLite database on startup with the `events` and `identity_mappings` tables. All subsequent stories can persist and query data.

## Acceptance Criteria (VERBATIM from prd.json — DO NOT paraphrase)

- Server uses better-sqlite3 (synchronous) or sqlite3 package to create/open a local 'minipanel.db' file in the project root
- Database file is gitignored
- A schema initialization function runs on server startup and creates tables if they do not exist
- Events table created with columns: id (INTEGER PRIMARY KEY AUTOINCREMENT), event_name (TEXT NOT NULL), device_id (TEXT), user_id (TEXT), timestamp (TEXT NOT NULL), properties (TEXT, stores JSON string)
- Identity mappings table created with columns: id (INTEGER PRIMARY KEY AUTOINCREMENT), device_id (TEXT NOT NULL UNIQUE), user_id (TEXT NOT NULL), created_at (TEXT NOT NULL)
- Server starts successfully with the database initialized and logs a confirmation message
- Typecheck passes

## Spec anchors (READ these sections if the briefing is unclear)

- CLAUDE.md § "Where to look" — DB convention: SQLite at project-root `minipanel.db`, schema in `server/src/db.ts`
- prd.json notes — "Use better-sqlite3 for simplicity. UNIQUE constraint on device_id in identity_mappings enforces one-device-to-one-user rule."

## Dependencies

- **Hard dependencies:**
  - US-001 — needs the server/ workspace scaffold
- **Soft dependencies:** None.
- **Unblocks:** US-003 (event ingestion needs tables)

## Expected artifacts (for the coder)

- `server/src/db.ts` — database connection + schema initialization
- `.gitignore` update — add `minipanel.db`
- Database migrations? Yes — creates `events` and `identity_mappings` tables via CREATE TABLE IF NOT EXISTS
- Config changes? No

## Test scope (for the validator)

- Primary test file: `server/src/__tests__/US-002.test.ts`
- Coverage the test MUST have (derived from acceptance criteria above):
  - Schema initialization creates both tables with correct columns
  - `events` table has: id, event_name, device_id, user_id, timestamp, properties
  - `identity_mappings` table has: id, device_id (UNIQUE), user_id, created_at
  - `minipanel.db` is in `.gitignore`
  - Server startup logs confirmation message
  - Calling init twice doesn't error (CREATE TABLE IF NOT EXISTS)
- Edge cases worth adversarial testing:
  - UNIQUE constraint on device_id in identity_mappings — inserting duplicate device_id should fail/be ignored
  - Properties column accepts NULL

## Cross-cutting conventions

- Workspaces: `server/` (backend, port 3001) + `client/` (frontend, port 5173). Never `backend/` or `frontend/`.
- DB: SQLite at project-root `minipanel.db`. Schema in `server/src/db.ts`.
- Tests live at `server/src/__tests__/<story_id>.test.ts` or `e2e/<story_id>.spec.ts`.
- UI: shadcn/ui components, design tokens from `.claude/skills/minipanel-design/references/`.
